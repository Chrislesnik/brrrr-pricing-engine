[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dscr-sdk"
version = "0.0.1"
description = "Python SDK for the dscr.ai API"
requires-python = ">=3.9"
dependencies = ["httpx>=0.27.0"]

[tool.setuptools.packages.find]
where = ["src"]
